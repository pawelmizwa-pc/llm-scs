{
  "name": "ollama-api-service",
  "version": "1.0.0",
  "description": "Fastify API service for Ollama models",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "fastify": "^4.25.2",
    "@fastify/cors": "^9.0.1",
    "ollama": "^0.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}